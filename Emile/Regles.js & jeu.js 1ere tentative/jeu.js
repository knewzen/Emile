//
var ScorePot = 0 // modifiable à partir de regles.js

Main ()

function CreationScorePlayer( ) {

}

 function InitTurn () {
//// dis au joueur c'est à toi de jouer : dés débloqués 5dés lancés

 }

 function Main ( ListPlayer) {
   CreationScorePlayer()
   for ( Player = ListPlayer[0], EndGame() === true , suivant sur la liste ) {
      InitTurn()
      var EndTurn = false
      while EndTurn === false // tant que tour non fini, syntaxe
        If ( LostRoll() ) {
          EndTurn = true
        }
        AffichageScorePot(DiceNotPlayed) // Affiche socre des dés cochés ( non rejoués)
        If ( EndTurn activé) {
            EndTurn()
        }
        If ( Suite() ) {
          // relance tout automatiquement
        }
        Else {
          // enable les checkbox des dés et bouton lancer et action lancer dés renvosi au début de boucle while
        }
   }
 }
